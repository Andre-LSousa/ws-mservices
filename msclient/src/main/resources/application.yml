spring:
  application:
    name: msclient

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://ms-eureka-user:ms-eureka-user-password@localhost:8761/eureka

  instance:
    hostname: localhost
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}